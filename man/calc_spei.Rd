% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_spei.R
\name{calc_spei}
\alias{calc_spei}
\title{Calculate Standardized Precipitation-Evapotranspiration Index (SPEI)}
\usage{
calc_spei(
  climate_data,
  scales = c(1, 3, 6),
  eto_method = "hargreaves",
  lat = 31.5
)
}
\arguments{
\item{climate_data}{A tibble with columns \code{date}, \code{precip}, \code{temp},
and optionally \code{eto}.}

\item{scales}{Integer vector. Time scales in months. Default: \code{c(1, 3, 6)}.}

\item{eto_method}{Character. Method to estimate ETo if not provided:
\code{"hargreaves"} (default) or \code{"thornthwaite"}.}

\item{lat}{Numeric. Latitude in decimal degrees (needed for Hargreaves ETo).
Default: \code{31.5} (central Morocco).}
}
\value{
A tibble with \code{date} and for each scale \code{spei_<scale>}
(numeric) and \code{class_<scale>} (character).
}
\description{
Computes a simplified SPEI using the climatic water balance
(Precipitation - Potential ETo) and standardizes using a log-logistic
or normal distribution.
}
\examples{
data(morocco_climate)
spei_result <- calc_spei(morocco_climate, scales = c(1, 3))
head(spei_result)

}
