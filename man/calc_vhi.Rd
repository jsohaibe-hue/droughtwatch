% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ndvi_vhi.R
\name{calc_vhi}
\alias{calc_vhi}
\title{Calculate Vegetation Health Index (VHI)}
\usage{
calc_vhi(ndvi, lst = NULL, vhi_weight = 0.5)
}
\arguments{
\item{ndvi}{SpatRaster. NDVI raster stack (values 0–1).}

\item{lst}{SpatRaster or NULL. Land Surface Temperature raster (optional).
If \code{NULL}, VHI is computed from VCI only.}

\item{vhi_weight}{Numeric (0–1). Weight of VCI in VHI.
VHI = vhi_weight * VCI + (1 - vhi_weight) * TCI. Default: \code{0.5}.}
}
\value{
A SpatRaster of VHI values (0–100) with classification attributes.
}
\description{
Computes the Vegetation Health Index from NDVI, combining vegetation
condition (VCI) with an optional temperature component (TCI).
}
\details{
VHI classes:
\itemize{
\item 0–10  : Extreme stress
\item 10–20 : Severe stress
\item 20–40 : Moderate stress
\item 40–60 : Moderate health
\item > 60  : Good health
}
}
\examples{
ndvi <- download_ndvi("2020-01-01", "2020-12-31", source = "synthetic")
vhi  <- calc_vhi(ndvi)
terra::plot(vhi)

}
