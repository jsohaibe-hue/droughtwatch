% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_alerts.R
\name{generate_alerts}
\alias{generate_alerts}
\title{Generate Drought Alerts}
\usage{
generate_alerts(
  drought_result,
  index_col = NULL,
  thresholds = c(moderate = -1, severe = -1.5, extreme = -2),
  region = "Study area"
)
}
\arguments{
\item{drought_result}{List returned by \code{\link{detect_drought}}.}

\item{index_col}{Character. Name of the index column in the series.
Default: auto-detected.}

\item{thresholds}{Named numeric vector. Alert thresholds for each level.
Default: \code{c(moderate = -1.0, severe = -1.5, extreme = -2.0)}.}

\item{region}{Character. Name of the region for the alert message.
Default: \code{"Study area"}.}
}
\value{
A tibble with columns: \code{date}, \code{index_value},
\code{alert_level}, \code{message}.
}
\description{
Generates a structured table of current and recent drought alerts based
on the latest index values and detected episodes.
}
\examples{
data(morocco_climate)
spi  <- calc_spi(morocco_climate, scales = 3)
dr   <- detect_drought(spi)
alerts <- generate_alerts(dr, region = "Beni Mellal-Khenifra")
print(alerts)

}
