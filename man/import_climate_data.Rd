% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_climate_data.R
\name{import_climate_data}
\alias{import_climate_data}
\title{Import Climate Data}
\usage{
import_climate_data(
  file = NULL,
  format = NULL,
  date_col = "date",
  precip_col = "precip",
  temp_col = "temp",
  eto_col = NULL,
  fill_missing = "interpolate",
  sheet = 1,
  data = NULL
)
}
\arguments{
\item{file}{Character. Path to the input file (CSV or Excel).}

\item{format}{Character. File format: \code{"csv"} or \code{"excel"}.
If \code{NULL} (default), the format is inferred from the file extension.}

\item{date_col}{Character. Name of the date column. Default: \code{"date"}.}

\item{precip_col}{Character. Name of the precipitation column. Default: \code{"precip"}.}

\item{temp_col}{Character. Name of the temperature column. Default: \code{"temp"}.}

\item{eto_col}{Character or NULL. Name of the ETo column. Default: \code{NULL} (optional).}

\item{fill_missing}{Character. Method to fill missing values: \code{"interpolate"},
\code{"mean"}, or \code{"none"}. Default: \code{"interpolate"}.}

\item{sheet}{Integer or character. For Excel files, the sheet to read. Default: \code{1}.}
}
\value{
A tibble with columns: \code{date} (Date), \code{precip} (numeric),
\code{temp} (numeric), and optionally \code{eto} (numeric).
}
\description{
Imports climate data (precipitation, temperature, optional ETo) from
CSV or Excel files, handles date parsing and missing values.
}
\examples{
# Using the built-in example dataset path
data_path <- system.file("extdata", "climate_example.csv", package = "droughtwatch")
if (file.exists(data_path)) {
  climate <- import_climate_data(data_path)
  head(climate)
}

# Simulate data directly
climate <- import_climate_data(data = morocco_climate)
head(climate)

}
